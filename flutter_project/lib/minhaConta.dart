import 'package:flutter/material.dart';

class MinhaContaScreen extends StatelessWidget {
  const MinhaContaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Conteúdo da Minha Conta"),
      ),
    );
  }
}
